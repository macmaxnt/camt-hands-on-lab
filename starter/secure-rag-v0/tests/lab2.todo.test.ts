import test from "node:test";

// These are acceptance targets for the learner's secure-rag-v2 milestone, not baseline behaviour.
test.skip("TODO Lab 2: a public user cannot retrieve, inspect, download, or cite staff data", () => {});
test.skip("TODO Lab 2: direct prompt injection is refused before retrieval and provider invocation", () => {});
test.skip("TODO Lab 2: synthetic PII is never sent to NVIDIA Build or audit storage", () => {});
test.skip("TODO Lab 2: poisoned corpus chunks are excluded from LLM context", () => {});
test.skip("TODO Lab 2: citations outside the authorized context are rejected", () => {});
test.skip("TODO Lab 2: audit events never store raw prompts or PII", () => {});
